> This file is maintained by AI to track progress. Update it as the project evolves.

---

# Production Readiness Backlog

## P0 — Critical Risk

| Issue | Status | Related Files | Details | Notes |
|---|---|---|---|---|
| DAG fan-in (join) correctness is not guaranteed | Resolved | src/modules/event/event-router.service.ts, src/modules/event/event-router.service.spec.ts | Added fan-in gating in EventRouter: downstream step is dispatched only when all direct parent steps for that node are completed in the same execution. | 2026-03-20: Added regression tests for join dispatch blocking/unblocking. |
| Branch flow can leave execution stuck indefinitely | Resolved | src/modules/worker/handlers/branch.handler.ts, src/modules/event/event-router.service.ts | Added fail-fast branch validation (no match + no default throws) and dead-end routing compensation in EventRouter when branch output does not map to any outgoing edge. | 2026-03-20: Added regression tests in branch.handler.spec.ts and event-router.service.spec.ts. |
| Cancel does not fully stop in-flight processing | Resolved | src/modules/execution/execution.service.ts, src/modules/execution/step-state.service.ts, src/modules/event/event-router.service.ts, src/modules/worker/consumer.service.ts | Added hard cancellation guards across worker and orchestrator paths: cancel now marks queued/running step executions as skipped; worker drops stale jobs when step cannot transition to running; event router ignores late step results for non-running executions and blocks retries after cancellation. | 2026-03-20: Added regression tests in execution.service.spec.ts, step-state.service.spec.ts, and event-router.service.spec.ts. |
| Idempotency is not domain-safe under concurrency | Resolved | src/modules/execution/execution.service.ts, src/modules/execution/execution.schema.ts, src/modules/execution/execution.service.spec.ts | Replaced check-then-insert with atomic insert + duplicate key handling; scoped idempotency uniqueness to owner_id + workflow_id + idempotency_key via compound partial unique index; normalized incoming keys to avoid whitespace/empty-key inconsistencies. | 2026-03-20: Added regression tests for duplicate-key conflict and key normalization. |
| Running executions are affected by workflow edits | Resolved | src/modules/execution/execution.schema.ts, src/modules/execution/execution.service.ts, src/modules/event/event-router.service.ts, src/modules/event/event-router.service.spec.ts | Added `workflow_snapshot` to execution records at trigger time and switched EventRouter to route/retry against the pinned snapshot. Added backward-compatible fallback to current workflow only when snapshot is absent (legacy executions). | 2026-03-20: Added regression test to verify in-flight execution remains pinned even if workflow is edited later. |
| Retry/backoff is process-memory based | Resolved | src/modules/event/event-router.service.ts, src/modules/worker/consumer.service.ts, src/shared/interfaces/step-job.interface.ts, src/modules/event/event-router.service.spec.ts | Replaced in-process `setTimeout` retry waiting with durable Pub/Sub-based deferred retries using `StepJob.notBefore`. Worker now defers early jobs via Pub/Sub ack-deadline extension (`message.modAck`) and processes when due, preserving retry intent across process restarts. | 2026-03-20: Added regression test to assert retry jobs carry valid `notBefore` metadata. |

## P1 — High Risk

| Issue | Status | Related Files | Details | Notes |
|---|---|---|---|---|
| At-least-once delivery handling is incomplete | Resolved | src/modules/worker/consumer.service.ts, src/modules/event/event-router.service.ts, src/modules/execution/step-state.service.ts, src/modules/event/event-router.service.spec.ts, src/modules/execution/step-state.service.spec.ts | Hardened state transitions and router logic for at-least-once semantics: `markRunning` is now atomic (`queued -> running` via conditional update), terminal transitions (`markCompleted`/`markFailed`) are conditional and idempotent (`running` only), and EventRouter ignores stale/duplicate result events by validating step status/attempt before retry/compensation/dispatch. | 2026-03-21: Added regression tests for duplicate completion and stale failed-result redelivery guards. |
| Scheduled triggers can duplicate across multiple app instances | Resolved | src/modules/scheduler/scheduler-lock.schema.ts, src/modules/scheduler/scheduler.module.ts, src/modules/scheduler/workflow-scheduler.service.ts, src/modules/scheduler/workflow-scheduler.service.spec.ts | Added MongoDB-backed distributed leader lease for scheduler ownership. Only the current leader instance registers/runs workflow cron jobs; followers clear local jobs and do not trigger. Leadership is renewed periodically and released on shutdown. | 2026-03-21: Added regression tests for leader/follower lock behavior. |
| Missing timeout/watchdog policies for stuck steps/executions | Resolved | src/modules/execution/execution.schema.ts, src/modules/execution/step-execution.schema.ts, src/modules/execution/step-state.service.ts, src/modules/execution/execution-watchdog.service.ts, src/modules/execution/execution-watchdog.service.spec.ts | Added explicit timeout policies and deadlines for execution/step lifecycles (`timeout_policy`, `timeout_at`, `timeout_ms`), plus a periodic watchdog that fails timed-out running steps/executions and triggers compensation to prevent indefinite stuck states. | 2026-03-21: Added regression tests for step-timeout and execution-timeout watchdog handling. |
| Compensation is generic and not business-action rollback | Resolved | src/modules/execution/compensate.service.ts, src/modules/execution/compensation-executor.service.ts, src/modules/execution/step-execution.schema.ts, src/modules/workflow/workflow.service.ts, src/modules/workflow/workflow.schema.ts, src/modules/workflow/dto/create-workflow.dto.ts, src/modules/event/execution-event.schema.ts, src/modules/execution/compensate.service.spec.ts, src/modules/execution/compensation-executor.service.spec.ts, src/modules/workflow/workflow.service.spec.ts | Phase 1 + Phase 2 complete: per-step compensation contract, reverse-order rollback for completed steps only, HTTP/noop compensation executor, compensation retry/backoff policy, idempotency key propagation (`x-compensation-idempotency-key`), durable compensation state tracking on step executions, and config validation hardening for compensation rules. | 2026-03-21: Completed phase 2 hardening and regression tests. |
| Webhook security lacks anti-replay and abuse controls | Resolved | src/modules/execution/webhook.controller.ts, src/modules/execution/execution.service.ts, src/modules/execution/webhook-nonce.schema.ts, src/modules/execution/webhook-rate-limit.schema.ts, src/modules/execution/execution.service.spec.ts | Added webhook hardening with required timestamp + nonce validation, durable replay protection via Mongo nonce registry (`webhook_nonces` TTL + unique index), HMAC-SHA256 request signature verification for secret-enabled webhooks (`x-webhook-signature` over timestamp/nonce/method/path/body-hash), and Mongo-backed per-workflow/IP fixed-window rate limiting (`webhook_rate_limits`). | 2026-03-21: Added regression tests for stale timestamp, replay nonce, invalid signature, and 429 rate-limit enforcement. |
| HTTP step has SSRF/outbound policy risk | Resolved | src/modules/worker/handlers/http.handler.ts, src/modules/worker/handlers/http.handler.spec.ts, src/modules/execution/compensation-executor.service.ts, src/modules/execution/compensation-executor.service.spec.ts | Hardened outbound HTTP policy end-to-end for worker steps and compensation HTTP calls: strict URL/protocol validation, optional host allowlist (`HTTP_STEP_ALLOWED_HOSTS`), and SSRF guardrails that block localhost/single-label internal hosts and private/reserved/link-local IP targets (including DNS-resolved addresses) unless explicitly allowed. | 2026-03-21: Added regression tests for localhost/private-IP/private-DNS resolution, allowlist enforcement, and protocol validation in both worker and compensation HTTP flows. |

## P2 — Medium Risk

| Issue | Status | Related Files | Details | Notes |
|---|---|---|---|---|
| Missing tenant quotas and business limits | Open | src/modules/execution/execution.controller.ts, src/modules/workflow/workflow.controller.ts | No domain limits for concurrent executions, trigger rate, or payload size per tenant/workflow, which can impact fairness and stability. | |
| Event retention and audit governance are not defined | Open | src/modules/event/event.service.ts, src/modules/event/execution-event.schema.ts | Event logs are append-only, but lifecycle policies (retention, indexing strategy, archival) and long-term query governance are not formalized. | |
| Step config contract validation is too generic | Open | src/modules/workflow/dto/create-workflow.dto.ts, src/modules/worker/step-executor.service.ts | DTOs validate general shape but do not enforce strict per-step-type config schemas (http/transform/store/branch), risking runtime misconfiguration. | |
| Execution query capabilities are limited for operations | Open | src/modules/execution/execution.controller.ts, src/modules/execution/execution.service.ts | Listing and retrieval endpoints lack production-grade filtering, pagination model, and operational search options for high-volume environments. | |
| Auth domain is still MVP-level | In progress | src/modules/auth/auth.service.ts, src/modules/auth/auth.controller.ts, src/modules/auth/auth-token.schema.ts, src/modules/auth/jwt.strategy.ts, src/infra/mail/mail.service.ts, src/modules/users/user.schema.ts, src/modules/users/users.service.ts | Phase delivered: register now requires email verification before login; one-time hashed verification/reset tokens with TTL (24h verify, 15m reset); forgot/reset password flow; resend verification endpoint; SMTP mail integration for Gmail App Password. Remaining gap: refresh-token rotation, session revocation, brute-force protections/lockout strategy. | 2026-03-22: Completed auth verification/reset/email phase; keep session revocation as follow-up hardening item. |

---
